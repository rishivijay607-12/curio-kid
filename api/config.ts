// This is a placeholder function to prevent Vercel build errors.
// It is not used by the application.
export default function handler(req, res) {
  res.status(200).json({ message: 'This endpoint is not in use.' });
}
